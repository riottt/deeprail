# Claude Adapter Entry

DeepRail semantics are defined by Standard / Workflow / Profile / Eval, not by this file.

For a project-local run, generate the active context:

```bash
./scripts/deeprail workflow start --project /path/to/project
```

Use the resolved communication language for interaction and artifact output language for human-readable artifacts.
