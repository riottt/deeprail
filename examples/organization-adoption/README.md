# Golden Path — Organization Adoption

`answers.json` を `deeprail assess` へ渡し、最初に解くべきBottleneckをBook / Standard / Workflowへ接続する例。

```bash
deeprail assess --answers examples/organization-adoption/answers.json --format md
```
