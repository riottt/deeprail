# Claude-compatible Adapter

Use the same language-neutral DeepRail workflows.

Recommended invocation pattern:

```bash
./scripts/deeprail workflow start --project /path/to/project
```

Place the generated runtime context in the active Claude session / project instruction path appropriate to your environment. Do not duplicate DeepRail semantics into a separate Claude-only standard.
