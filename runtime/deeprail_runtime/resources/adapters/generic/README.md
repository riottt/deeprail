# Generic Agent Adapter

1. Resolve `.deeprail/project.yaml`
2. Select the active DeepRail workflow
3. Build runtime context with `./scripts/deeprail workflow <name> --project <path>`
4. Supply that context to the agent
5. Require evidence and transition decisions according to the workflow contract
