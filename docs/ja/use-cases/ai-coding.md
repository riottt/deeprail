# AI CodingをTeamへ導入したい

## 最初にやること
1. `lightweight` または `standard` Profileで1つのFeatureを選ぶ
2. `discover → specify → execute → verify → reinvest` を通す
3. AIの自己申告ではなくEvidenceを残す
4. Human InterventionをReinvestment Candidateへ戻す

おすすめ: `A2 × EA1 × S4` 付近から開始し、実測で調整する。
