# Bringing AI Coding to a Team

## First steps
1. Pick one feature with the `lightweight` or `standard` profile
2. Run it through `discover → specify → execute → verify → reinvest`
3. Record evidence, not AI self-reports
4. Feed human interventions back as Reinvestment Candidates

Recommended: start around `A2 × EA1 × S4` and adjust from measurement.
